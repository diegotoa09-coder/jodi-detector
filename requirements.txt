# Versiones de escritorio (la web no necesita nada de esto)
pywebview>=5.0      # ventana de escritorio para detector_web.py
nltk>=3.8           # tokenización y stopwords
spacy>=3.7          # morfología (opcional; tras instalar: python -m spacy download es_core_news_sm)
python-docx>=1.1    # leer archivos .docx
pymupdf>=1.23       # leer archivos .pdf
